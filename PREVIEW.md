# 4.2.2 使用说明

- 双击 **KMCounter.exe**：打开主界面并开始正式统计，无需启动脚本或额外安装 AutoHotkey。
- 关闭主窗口后继续驻留托盘；托盘“退出”保存并结束。
- 配置和统计数据保存在 exe 同目录的 KMCounter.ini，更早明细存放在 history/。请将程序放在可写目录。
- 新建的开机启动快捷方式使用 --background，只驻留托盘。开发用的 --demo 参数仍可查看合成数据，但不是正式启动方式。

## 界面

默认主界面约 871 × 310 物理像素（不含系统标题栏，随原始布局设置变化）。保持紧凑宽度，增加高度和留白；键帽使用 4px 圆角和平整色面。设置窗口先选择 Dark / Light，再选择该模式下的四套配色；缩略图展示完整的背景、键帽和选中状态。

左上角齿轮打开设置：第一级为深色 Dark、浅色 Light；第二级提供当前模式下的四套完整配色。Dark 保留冰川青、翡翠、熔岩、琥珀；Light 提供樱粉、薄荷绿、雾蓝、淡紫，每套的背景、统计区、空闲键和热力键帽保持同一色系。单击预览立即应用并保存，与托盘同步；两种模式各自记住上次的配色。重复点击当前模式不重置颜色，切换不丢弃尚未保存的数值输入；取消按钮只取消数值设置。齿轮和配色按钮支持 Tab 聚焦、空格选择。时间范围始终使用独立灰阶。

设置预览改为预先生成的完整位图，按钮在自身绘制回调中直接显示缓存图像，避免切换时出现空白色块。旧版樱粉和薄荷绿配置升级后分别映射到 Light 的粉色和绿色；统计数据格式不变。

键帽由 GDI+ 直接绘制小圆角和平整色面，取消渐变高光和立体描边，保留约 5px 的视觉间距。时间范围采用灰色分段控件，在齿轮按钮右缘与色标左缘之间居中。右上方色标下用单行图标与数字展示键击、左键、右键和移动距离；统计区采用低对比度圆角底框，深色主题略浅、浅色主题略深；整组对齐色标左右边界和数字键区右边界，数字使用统一的小字号，并与键盘保持独立留白。底部只保留一行日期与数据覆盖说明。顶部齿轮和范围栏下移 8px，底部说明上移 8px，窗口底部留白相应缩短。悬停统计可查看鼠标细项。Windows 11 支持深色标题栏和圆角窗口，旧版 Windows 保留普通边框。本版使用纯色背景。

深色主题热度由暗变亮，浅色主题由浅变深，均按明度单调递进。Light 四套配色的高频端均限制在柔和粉彩区间（相对明度不低于 0.34），不出现深紫或墨绿。零次按键使用配套的独立底色，悬停可查看精确次数；右上方色条从低频到高频，悬停显示当前范围峰值。配色选择会自动保存，演示模式除外。

着色采用 `ln(1 + count) / ln(1 + maxCount)`；最大值取当前范围内已显示按键的峰值。单次按键也可显示，无需积累 100 次。不同范围各自归一化，因此不能仅凭颜色比较不同范围的绝对次数。

## 时间范围与存储

- 今日：当天实时数据。
- 本周：周一到今天。
- 本月、今年：自然月、自然年起点到今天。
- 历史总计：独立保存的 `total` 段；不从保留天数推算。

主文件 `KMCounter.ini` 默认保留最近 366 个日期（含今天）。升级时尊重已有 `history.storage` 配置；该值现在表示主文件的活跃明细窗口，而非数据删除期限。

更早的每日明细归档至 `history/YYYY.ini`，仍按日期保存完整快照，不合并丢失细节。年度分片随时间增长，通常一年只有几百 KB。同一天覆盖对应日期段，不重复追加累计。年度文件写入、核对并原子替换成功后，才移除主文件中的旧日期段；首次归档保留 `KMCounter.ini.pre-archive.bak`。中断后可以安全重试。`total` 段不受归档影响。

每分钟以及正常退出时，将当前计数和总计写入临时文件，核对后替换主文件。可用日期会显示在范围下方；旧版已删除的历史日明细无法重建，但原有总计仍保留。没有明细的日期按缺失处理，不伪造零数据。

## 开发与检查

原项目未附带的 OSDTIP、ImagePut、CtlColors、ImageButton、UseGDIP 等外部界面依赖已移除，界面用 Windows 原生控件实现。仍使用 AutoHotkey v1，而非 v2。

```powershell
# 下载便携运行时，不安装到系统；已下载的环境可跳过
.\tools\SetupRuntime.ps1
# 数据、归档及原生窗口几何检查
.\tools\Test.ps1
# 构建项目根目录的 KMCounter.exe
.\tools\Build.ps1
```

也可直接使用 AutoHotkey v1.1.37.02 运行 `KMCounter.ahk --demo`。`--self-test` 和 `--smoke` 只使用测试数据；测试前请退出正在运行的正式实例，因为脚本使用单实例模式。

已验证日期边界、对数映射、范围汇总、实时计数、归档重试、独立累计、原子保存，以及 40 种主题/范围/色谱组合、托盘主题选中状态、原生 WM_DRAWITEM 绘制成功和按键矩形无重叠。另验证默认按钮状态切换后齿轮仍有可见绘制、主题预览点击同步托盘、未保存输入保留，以及隐藏设置窗口的文字颜色更新。使用实际绘制函数离屏生成的合成数据预览位于 screenshots/redesign/，已目视检查；这些图片不是桌面截图。

本轮非官方设计参考：[Tonsky 的图标分析](https://tonsky.me/blog/tahoe-icons/)、[Mac 应用开发者讨论](https://www.reddit.com/r/macapps/comments/1ubneer/what_makes_a_macos_app_feel_truly_native_and/)。采用少而明确的图标、可预期的设置入口、居中对齐和克制的层次；不叠加装饰性图标或强模糊。

视觉美学参考：[Anthony Hobday 的视觉设计指南](https://anthonyhobday.com/sideprojects/quickstart/visualdesign)、[Evil Martians 的 macOS 视觉分析](https://evilmartians.com/chronicles/how-to-make-absolutely-any-app-look-like-a-macos-app)。采用一致间距、数字与标签分层、近黑文字、克制的强调色和略高的键帽比例。使用 Windows 可用字体保留清晰度。

深色主题调整为柔和石墨灰，四套深色色谱降低饱和度与峰值亮度，仍保持热度的明度顺序和文字对比度。

「托盘 → 设置 → 语言 / Language」提供中文和 English，即时更新主界面、常规设置、主题与色谱菜单及提示；正式模式保存选择，演示模式不写入配置。切换不重启程序、不重置统计，也不丢弃设置窗口中尚未保存的输入。英文渲染预览为 screenshots/redesign/dark-english.png。

统计区保持 256px 宽度，右边缘与数字键区及色标对齐，高度由 50px 缩至 32px；色标和统计条整体下移，统计条底边与 Esc / F1–F12 行的键帽底边对齐。单行依次显示键盘、左键高亮鼠标、右键高亮鼠标和移动轨迹图标，以及相应数字；距离保留 m 单位。左右鼠标键独立显示当前时间范围的次数；悬停图标或数字可查看包含键击、鼠标各键、滚轮及移动距离的精确值。

统计数字始终采用统一的 7.5pt 逻辑字号，按实际字体宽度共享空间；完整数字放不下时，优先将占用空间较大的数值缩写为 k（千），例如 12,345 → 12.3k。整行仍不足时省略小数；极端大值可使用以 k 为单位的指数格式。切换时间范围不会缩小字号，缩写只影响显示，保存数据和悬停精确值不受影响。

托盘中的“布局定制 / Custom Layout”只弹窗显示联系邮箱 huizhang8080@gmail.com，不打开邮件应用、浏览器或旧项目页面。提示支持中文和 English。
